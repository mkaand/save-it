@extends('admin.layout')
@section('title','Reports')
@section('content')<main class="panel"><h1>Operational reports</h1><p class="muted">Reports use the existing application mail configuration. Duplicate scheduled periods are never sent twice.</p><form method="post" action="{{ route('admin.reports.update') }}">@csrf @method('PUT')<div class="grid"><div><label>Mode</label><select name="mode"><option value="disabled" @selected($reports['mode']==='disabled')>Disabled</option><option value="daily" @selected($reports['mode']==='daily')>Daily</option><option value="weekly" @selected($reports['mode']==='weekly')>Weekly</option></select></div><div><label>Recipient</label><input type="email" name="recipient" value="{{ old('recipient',$reports['recipient']) }}"></div><div><label>Timezone</label><input name="timezone" value="{{ old('timezone',$reports['timezone']) }}" required></div><div><label>Delivery time</label><input type="time" name="time" value="{{ old('time',$reports['time']) }}" required></div><div><label>Weekly weekday</label><select name="weekday">@foreach([1=>'Monday',2=>'Tuesday',3=>'Wednesday',4=>'Thursday',5=>'Friday',6=>'Saturday',7=>'Sunday'] as $number=>$day)<option value="{{ $number }}" @selected($reports['weekday']===$number)>{{ $day }}</option>@endforeach</select></div></div><div class="action-grid"><button class="button button-primary">Save reports</button></div></form><div class="action-grid"><form method="post" action="{{ route('admin.reports.send') }}">@csrf<button class="button" type="submit">Send report now</button></form></div></main>@endsection
