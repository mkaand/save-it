@extends('admin.layout')
@section('title','Profile')
@section('content')<main class="panel"><h1>Profile</h1><form method="post" action="{{ route('admin.profile.update') }}">@csrf @method('PUT')<label>Username</label><input name="username" value="{{ old('username',$admin->username) }}" required><label>Email</label><input name="email" type="email" value="{{ old('email',$admin->email) }}" required><div class="actions"><button class="button button-primary">Save profile</button></div></form><form class="section" method="post" action="{{ route('admin.profile.password') }}">@csrf @method('PUT')<h2>Change password</h2><label>Current password</label><input name="current_password" type="password" autocomplete="current-password" required><label>New password</label><input name="password" type="password" autocomplete="new-password" required><label>Confirm new password</label><input name="password_confirmation" type="password" autocomplete="new-password" required><div class="actions"><button class="button button-primary">Change password</button></div></form></main>@endsection
