@extends('admin.layout')
@section('title','GeoIP')
@section('content')<main class="panel"><h1>Country analytics</h1><p class="muted">No external IP-geolocation service is used. Only an aggregate country code is recorded.</p><form method="post" action="{{ route('admin.geoip.update') }}">@csrf @method('PUT')<div class="grid"><div><label>Source</label><select name="mode"><option value="none" @selected($geoip['mode']==='none')>None (Unknown)</option><option value="trusted_header" @selected($geoip['mode']==='trusted_header')>Trusted country header</option><option value="maxmind" @selected($geoip['mode']==='maxmind')>Local MaxMind GeoLite2</option></select></div><div><label>Trusted header name</label><input name="header" value="{{ old('header',$geoip['header']) }}" placeholder="CF-IPCountry"><span class="secret-note">Required for Trusted country header. The default is CF-IPCountry; values must be ISO alpha-2.</span>@error('header')<span class="field-error">{{ $message }}</span>@enderror</div><div><label>Local MaxMind database path</label><input name="maxmind_path" value="{{ old('maxmind_path',$geoip['maxmind_path']) }}"><span class="secret-note">Missing or unreadable files resolve safely to Unknown.</span></div></div><div class="action-grid"><button class="button button-primary">Save GeoIP</button></div></form></main>@endsection
